[project]
name = "image-mcp"
version = "0.1.0"
description = "Universal Image MCP Server - Support multiple AI models for image understanding and generation"
authors = [
    {name = "鸵鸟居士", email = "ostrich@example.com"}
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "mcp>=0.9.0",
    "httpx>=0.27.0",
    "Pillow>=10.0.0",
    "python-dotenv>=1.0.0",
    "zai-sdk>=0.2.2",
]

# Build system - optional, for direct pip install without building
# [build-system]
# requires = ["hatchling"]
# build-backend = "hatchling.build"
#
# [tool.hatch.build.targets.wheel]
# packages = ["src"]
